import { SxProps } from '@mui/material';

export const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    padding: '8px '
  } as SxProps,

  listBox: {
    width: 'content'
  } as SxProps,

  scrollContainer: {
    maxHeight: 220,
    overflowY: 'auto'
  } as SxProps,

  divider: {
    my: 1
  } as SxProps
};
