import { backgroundColors } from '../../design-system/all-components/theme/colors';

export const styles = {
  mobileContainer: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: '50%',
    border: `4px solid ${backgroundColors.white}`
  }
};
